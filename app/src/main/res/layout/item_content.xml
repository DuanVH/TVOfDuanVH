<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:focusable="true"
    android:id="@+id/itemContent"
    android:focusableInTouchMode="true"
    android:background="@drawable/bg_item_content"
    android:layout_margin="@dimen/layout_margin_content">

    <ImageView
        android:id="@+id/iv_icon_content"
        android:layout_width="@dimen/layout_width_iv_content"
        android:layout_height="@dimen/layout_height_iv_content"
        app:layout_constraintBottom_toTopOf="@+id/tv_title_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        app:srcCompat="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/tv_title_content"
        android:layout_width="@dimen/layout_width_tv_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/margin_top_content"
        android:gravity="center"
        android:maxLines="2"
        android:text="@string/tv_content"
        android:textColor="@color/colorWhite"
        android:textSize="@dimen/text_size_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/iv_icon_content" />
</android.support.constraint.ConstraintLayout>